<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
        <OutputType>Exe</OutputType>
        <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    </PropertyGroup>
    <PropertyGroup>
        <Title>shapito</Title>
        <Description>shapito does the thing!</Description>
    </PropertyGroup>
    <PropertyGroup Condition="'$(Configuration)'=='Release'">
        <Optimize>true</Optimize>
        <Tailcalls>true</Tailcalls>
    </PropertyGroup>
    <ItemGroup>
        <Content Include="Task.md" />
        <Compile Include="AssemblyInfo.fs" />
        <None Include="App.config" />
        <Compile Include="OOPList.fs" />
        <Compile Include="MyList.fs" />
        <Compile Include="Convertor.fs" />
        <Compile Include="Homework1.fs" />
        <Compile Include="QTree.fs" />
        <Compile Include="Main.fs" />
        <Content Include="Thoughts.md" />
    </ItemGroup>
    <Import Project="..\..\.paket\Paket.Restore.targets" />
</Project>
